<h1>Listing bases</h1>

<table>
  <tr>
    <th>Descrption</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bases.each do |basis| %>
  <tr>
    <td><%= basis.descrption %></td>
    <td><%= link_to 'Show', basis %></td>
    <td><%= link_to 'Edit', edit_basis_path(basis) %></td>
    <td><%= link_to 'Destroy', basis, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Base', new_basis_path %>
